name: example
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  d1_dart:
    path: ../

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
